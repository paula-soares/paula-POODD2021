/// ja aprendemos que 
// classes são o principio dos objetos, é  a partir das classes que através do processo
// de instanciar os objetos se fundam

// toda classe/objeto tem:
// Atributos, metodos e estados. 
// atributos: o que tem, o que compõe a "materia" do objeto
// metodo: o que faz, os que esses componentes produzem
// estado: é o contexto, o estado em que se encontra determinado objeto

// para iniciar vamos pensar um ojeto pessoa, onde nome e idade são atributos
//detalhe que deve-se declarar o atributo usando aspas e no final virgula okokok

let pessoa = {
    "nome":"paula",
    "idade":28,

}

console.log(pessoa);

/// podemos acessar o objeto como todo ou somente o atributo em especifico

console.log(pessoa);
console.log(pessoa.nome);
console.log(pessoa.idade);


depositar(contac,5887);
console.log(contac.saldo);